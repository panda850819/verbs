.DS_Store

# secrets — never commit. Real secrets live outside the repo
# (~/.config/pandastack/secrets.env); these patterns guard against anything
# being dropped into the repo by mistake.
*.env
secrets.env
.env.local

# local capability-map state (machine-generated, host-specific)
.pandastack/local/
