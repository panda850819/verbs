---
date: 2026-07-02
issue: 137
branch: docs/137-count-ssot
type: plan
---

# Plan — count-ssot (#137)

> Remove hand-synced current-state skill counts from living prose; `manifest.toml` is the single source (its derived JSONs are already generated by `scripts/pandastack sync`). Extend the linter so the class cannot creep back. Base: chore/v3.4.0-fable5-harness-cut (8321ff4).

### count-ssot-T01 — strip current-state counts from living prose
- scope: README.md, CLAUDE.md, RESOLVER.md
- goal: current-state skill-count claims come out of prose; text points at `manifest.toml` instead. Historical sections (RESOLVER version-cut summaries / provenance / aliases, CHANGELOG, dated files under evals/, tests/resolver-golden.md) stay untouched. Exact edits:
  - README.md ~line 7: "The stack is **19 skills** focused on dev, writing, and knowledge workflows, tiered into 15 core (markdown-only, fresh-clone runnable) and 4 ext (publicly installable CLI)." → "The stack is a compact skill set focused on dev, writing, and knowledge workflows, tiered **core** (markdown-only, fresh-clone runnable) and **ext** (publicly installable CLI) in `manifest.toml`."
  - README.md ~line 13: "The current surface is **19 skills (15 core / 4 ext)**, after successive scope-tightening" → "The current surface is the tier list in `manifest.toml`, after successive scope-tightening"
  - README.md ~line 38: "- 15 core skills runnable on this clone with no external CLI" → "- core skills runnable on this clone with no external CLI"
  - README.md ~line 39 (if it carries a count): "- 4 extension skills with the exact `brew install` / `npm install -g` to enable each" → "- extension skills with the exact `brew install` / `npm install -g` to enable each"
  - README.md ~line 261: "19 skills grouped by lifecycle (15 core / 4 ext — see `manifest.toml`)." → "Skills grouped by lifecycle (core / ext tiers — see `manifest.toml`)."
  - CLAUDE.md line 3: "19 skills (15 core / 4 ext — see `manifest.toml`) in engineering / productivity / writing / meta buckets, plus 3 documented lifecycle compositions." → "Skills tiered core / ext (see `manifest.toml`) in engineering / productivity / writing / meta buckets, plus 3 documented lifecycle compositions."
  - RESOLVER.md line 9: "pandastack ships **19 skills** (15 core + 4 ext)." → "pandastack ships the skills cataloged in `manifest.toml` (core + ext tiers)."
  - Do NOT touch .claude-plugin/*.json or .codex-plugin/*.json — `scripts/pandastack sync` owns their description counts.
- acceptance: `grep -nE "[0-9]+ skills? \(|[0-9]+ (core|ext)\b" README.md CLAUDE.md` returns nothing; same grep on RESOLVER.md returns matches only inside historical sections (from "## Private supplement" / version-cut summaries downward), none in lines 1-20
- depends-on: none
- status: todo

### count-ssot-T02 — linter catches the class
- scope: scripts/lint-manifest-sync.sh
- goal: the stale-claims scan also fails on reintroduced current-count phrases. Keep the existing scan_docs block; add a pattern for hardcoded current-count claims — e.g. `[0-9]+ skills? \(` and `\([0-9]+ core` — over the existing scan_docs set, plus a separate check of RESOLVER.md's living header only (first ~20 lines, e.g. `head -20 RESOLVER.md | grep -E ...`) so RESOLVER's historical sections stay exempt. Match the script's existing style (fail=1 accumulation, FAIL: messages).
- acceptance: appending the line "pandastack ships 19 skills (15 core / 4 ext)" to README.md makes `bash scripts/lint-manifest-sync.sh` exit 1 with a stale-claims FAIL (revert the injection after checking); on the clean tree the script exits 0
- depends-on: count-ssot-T01
- status: todo

### count-ssot-T03 — suite green
- scope: verification only, no new files
- goal: the whole deterministic suite passes with the edits.
- acceptance: `bash tests/run-all.sh` reports 0 failed
- depends-on: count-ssot-T02
- status: todo
