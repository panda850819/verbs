[context]
name        = "personal:knowledge-manager"
description = "Vault hygiene, feed intake, note distillation, and knowledge publishing"
domain      = "personal"
private     = false

[persona]
agent         = ""   # no persona
voice_override = ""

[flow]
# v2.2: research is no longer a first-class flow — it's a knowledge variant.
main = "knowledge"
side = ["retro"]

[skills]
public = [
  "pandastack:ship",             # /ship knowledge <note> — Close + Extract + Backflow on knowledge/ note
  "pandastack:deepwiki",         # GitHub repo docs for technical deep-dives
  "pandastack:grill",            # adversarial mode to scope research questions
  "pandastack:retro-week",
  "pandastack:retro-month",
  "pandastack:done",
]
private = []  # tier=personal skills (curate-feeds / bird / brief-morning / evening-distill) load via private overlay only

[memory]
namespace    = "personal/knowledge"
firewall_from = ["work/yei/*"]

[sources]
vault = "personal-vault"

[mcp]
deny = [
  "mcp__claude_ai_Linear__*",     # work issue tracker — only allowed in work contexts
  "mcp__claude_ai_Atlassian__*",  # work issue tracker — only allowed in work contexts
]

[notes]
# Brain-first lookup rule applies: scan vault (rg / find) before fetching any URL or writing new notes.
# Absorb-first: if 60%+ overlap with existing note, update existing rather than creating new.
# /ship knowledge runs per note at distill time. Vault hygiene (orphans / stale / dead redirects) is on-demand via direct file scans.
